@model UserProfileViewModel
<html>
    <nav class="navbar navbar-inverse  navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                </button>

            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="@Url.Action("Index","Home")">Home</a></li>
                    <li ><a href="@Url.Action("List","Asset")">Asset <span class="sr-only"></span></a></li>
                    <li><a href="@Url.Action("Index","TaskQueue")">Task Queue</a></li>
                    <li><a href="@Url.Action("Index", "Reporting")">Reporting <span class="sr-only"></span></a></li>
                    <li class="active"><a href="@Url.Action("Index", "UserProfile")">User Profile <span class="sr-only">(current)</span></a></li>
                </ul>
            </div>
        </div>
    </nav>
   
    <div class="container mx-auto" style="width: 500px;">
     <h2>@ViewBag.Title</h2>
      <div class="form-group">
        <label asp-for="FirstName" ></label>
        <input id="firstname" asp-for="FirstName" class="form-control" readonly="readonly"/>
        <span asp-validation-for="FirstName"></span>
      </div>

       <div class="form-group">
        <label asp-for="LastName"></label>
        <input id="lastname" asp-for="LastName" class="form-control" readonly="readonly"/>
        <span asp-validation-for="LastName"></span>
      </div>

       <div class="form-group">
        <label asp-for="Email"></label>
        <input id="email" asp-for="Email" class="form-control" readonly="readonly"/>
        <span asp-validation-for="Email"></span>
      </div>
    </div>

</html>

<script type="text/javascript">
    $(document).ready(function () {
        var firstname = document.querySelector("#firstname");
        var lastname = document.querySelector("#lastname");
        var email = document.querySelector("#email");

        firstname.addEventListener("click", edit, false);
        lastname.addEventListener("click", edit, false);
        email.addEventListener("click", edit, false);

        firstname.addEventListener("click", edit, false);
        lastname.addEventListener("click", edit, false);
        email.addEventListener("click", edit, false);
    });
    function edit(e){
        var clickedItem = e.target;
        clickedItem.readOnly = false;
        console.log(clickedItem);
    }
    
</script>

   
